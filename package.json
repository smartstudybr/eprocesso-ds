{
  "name": "@eprocesso/design-system",
  "version": "1.0.0",
  "description": "Design System CLI para componentes React + Tailwind",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/cli && npm run build",
    "build:cli": "cd packages/cli && npm run build",
    "dev": "cd packages/cli && npm run dev",
    "publish:cli": "npm run build:cli && cd packages/cli && npm publish",
    "sync:registry": "node scripts/sync-registry.js",
    "install:all": "npm install && npm install --workspace=packages/cli && npm install --workspace=packages/registry"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "typescript": "^5.2.2",
    "tsx": "^4.6.0"
  }
}